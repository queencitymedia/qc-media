export default function Home() {
  return (
    <main>
      <h1>QC Media Home OK</h1>
      <p>If you can read this, the App Router root (/) is healthy.</p>
      <ul>
        <li>Try <code>/api/health</code> (should return JSON)</li>
        <li>Try <code>/api/offers</code> (your offers endpoint)</li>
      </ul>
    </main>
  );
}